<Window x:Class="ChauffeurJobManager.nextWorkingDay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChauffeurJobManager"
        mc:Ignorable="d"
        Title="Next Working Day" Height="209.339" Width="500">
    <Grid>
        <DataGrid x:Name="selectedTableDataGrid" HorizontalAlignment="Left" Margin="5,4,0,0" VerticalAlignment="Top" Height="178" Width="485" AutoGeneratingColumn="OnAutoGeneratingColumn" IsReadOnly="True">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="Copy" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</Window>
